# Environments & secrets
.env
.venv/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Data (vector store is derived state — never commit it)
data/chroma/

# Source documents for the corpus (large, often third-party/proprietary --
# e.g. scraped manuals; only the derived eval dataset YAML is versioned)
docs_corpus/

# IDE / OS
.vscode/
.idea/
.DS_Store
